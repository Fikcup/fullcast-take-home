import React, { useRef, useState } from "react";
import "./index.css";

interface FormProps {

}

const Form = (props: FormProps) => {
    return (
        <>
        </>
    );
};

export default Form;